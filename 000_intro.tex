% !TeX spellcheck = ru_RU
% !TEX root = vkr.tex

\section*{Введение}
\thispagestyle{withCompileDate}

Существуют два принципиально разных подхода к организации работы с данными в \db{} \todo{(расшифровка)}: \oltp{} (Online Transaction Processing, интерактивная транзакционная обработка) и \olap{} (Online Analytical Processing, интерактивная аналитическая обработка).

\postgresql{} ~--- реляционная \dbms{} с открытым исходным кодом, в основе которой лежат принципы \oltp{} \todo{(переформулировать)}.

В последнее время \todo{наблюдается тенденция по объединению \oltp{} и \olap{} подходов в \postgresql{} для создания единой платформы по работе с данными} \cite{postgresproOlapOltpPdfTalk}.

В данной работе рассматриваются существующие технологии организации \olap{}-обработки данных в \postgresql{} с использованием колоночного формата хранения данных \parquet{} и приводятся результаты проведения сравнительного анализа производительности различных подходов выполнения \olap{}-запросов в \postgresql{}.
