% !TeX spellcheck = ru_RU
% !TEX root = vkr.tex

\section{Описание решения}
% \todov{Здесь будет написано все про создание приложения для работы с кириллическими текстами}

\subsection{Требования к продукту}
    \label{subsec:task1}

    При исследовании проблематики предметной области был собран и проработан ряд требований, представленный ниже.

    \vspace{1em}

    \textbf{Бизнес-требования:}
    \begin{enumerate}
        \item обеспечение возможности ручного набора и редактирования кириллических текстов.
    \end{enumerate}

    \vspace{1em}

    \textbf{Функциональные требования:}
    \begin{enumerate}
        \item импорт и экспорт файлов в форматах \pdf{}, DOCX, TXT;
        \item генерация \pdf{} с использованием \TeX{};
        \item конвертация текста между шрифтами;
        \item встроенная клавиатура для ввода спецсимволов.
    \end{enumerate}

    \vspace{1em}

    \textbf{Нефункциональные требования:}
    \begin{enumerate}
        \item кроссплатформенность;
        \item интуитивно понятный интерфейс.
    \end{enumerate}


\subsection{Архитектура решения}
    \label{subsec:task2}

    Чтобы удовлетворить требованиям кроссплатформенности и простоты интерфейса, было принято решение реализовать инструмент в виде одностраничного веб-приложения (Single-Page App) \cite{jadhav2015single}.
    % TODOV: убрать ссылку? там же angular js...

    % TODOV: переформулировать "внешний вид" был разработан....
    Внешний вид (frontend) приложения разработан с помощью \vuejs{}, в то время как внутренняя логика (backend) написана на \python{} веб-фреймворке \django{}. Для реализации импорта и экспорта файлов использовались \python{}-библиотеки \docx{} и \pypdf{}.

    Компиляция \pdf{} файла с текстом, набранным пользователем, происходит по следующему сценарию (см. рис. \ref{fig:arch}):
    \begin{enumerate}
        \item пользователь набирает текст в поле редактора и настраивает параметры отображения (шрифт, размер, отступы) с помощью верхнего меню при необходимости;
        \item пользователь нажимает кнопку \enquote{Предпросмотр PDF} или комбинацию клавиш Ctrl+S;
        \item frontend получает содержимое поля редактора и параметры отображения текста, отправляет их на backend с помощью HTTP POST запроса на \todov{endpoint} \textbf{/api/compile/};
        \item backend принимает информацию, вставляет ее в шаблон структуры \TeX{} файла, предварительно обработав текст во избежание ошибок компиляции и отображения, и компилирует \pdf{} с помощью \xelatex{};
        \item при успешной компиляции итоговый \pdf{} отправляется на frontend и показывается пользователю в правой части рабочего пространства; при возникновении ошибки сообщение также направляется от backend-части на frontend и показывается пользователю.
    \end{enumerate}

    Остальные зависящие от внутренней логики сценарии (импорт, экспорт) работают по такому же принципу.

    % \todo{Используемые технологии:}
    % \begin{itemize}
    %     \item \todo{\nginx{}}
    % \end{itemize}

    \begin{figure}[h!]
        \centering
        \includegraphics[width=0.9\textwidth]{figures/arch_diagram1.png}
        \caption{Сценарий использования инструмента} %TODO: переименовать...
        \label{fig:arch}
    \end{figure}





\subsection{Особенности реализации}
    \label{subsec:task3}

    Приложение контейнеризировано с помощью \docker{}, а также создан \makefile{} с целями (targets), необходимыми для разработки, тестирования и развертывания. Для запуска инструмента на целевом сервере достаточно установить значение переменной \textbf{DJANGO\_SECRET\_KEY} в файле \textbf{./backend/.env} и вызвать команду \textbf{make deploy}.

    В приложении существуют конвертации двух видов:
    \begin{enumerate}
        \item между шрифтами;
        \item из кириллицы в глаголицу и обратно.
    \end{enumerate}
    Оба вида реализованы с помощью одного механизма, основанного на словарях и работающего во frontend-части приложения. Для конвертации между шрифтами используется следующая логика:
    \begin{enumerate}
        \item \todo{TODO}
    \end{enumerate}
    В случае конвертации текста из кириллицы в глаголицу (и обратно) подход проще: используется лишь словарь CyrillicToGlagolitic (и GlagoliticToCyrillic~--- обратно).

    Проблема отображения длинных слов (в древних кириллических рукописях текст писался без пробелов) была решена следующим образом: в случае, если длина слова превышает максимально допустимое значение, определенное в конфигурации приложения, само слово оборачивается в специальную \TeX{}-команду \textbf{\texttt{\textbackslash{}seqsplit\{\}}} из одноименного пакета, позволяющую автоматически разбивать набор символов на строки. Также было добавлено экранирование символов, специфичных для \TeX{}, во избежание ошибок компиляции (например, символ \texttt{\&} заменяется на \texttt{\textbackslash\&}).

    Внешний вид приложения представлен на рис. \ref{fig:ui}.

    \begin{figure}[h!]
        \centering
        \includegraphics[width=1.0\textwidth]{figures/ui.png}
        \caption{Внешний вид приложения}
        \label{fig:ui}
    \end{figure}

    В репозитории проекта\footnote{\url{https://github.com/aleksandr-yavorovskiy/cyrillic-editor/} (дата обращения: \DTMDate{2026-05-12}).} оформлен CI (Continuous Integration) с помощью \githubworkflow{}\footnote{\url{https://docs.github.com/en/actions/concepts/workflows-and-actions/workflows} (дата обращения: \DTMDate{2026-05-12}).}, в котором производится запуск линтеров, тестов и контейнеризация приложения.
