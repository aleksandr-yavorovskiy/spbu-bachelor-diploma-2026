% !TeX spellcheck = ru_RU
% !TEX root = vkr.tex

\section{Эксперимент}
\subsection{Условия эксперимента}
Тестовый стенд:
\begin{itemize}
    \item HUAWEI MateBook D15 BoDE-WXX9
    \item CPU: 11th Gen Intel(R) Core(TM) i5-1155G7 @ 2.50GHz
    \item Ядра: 8
    \item Зафиксированная частота процессора: 4000 GHz
    \item ОЗУ: 16GB
    \item ОС: Ubuntu 22.04.4 LTS
    \item \postgresql{} 18.1 (Ubuntu 18.1-1.pgdg22.04+2)
    \item \pgduckdb{} \todo{такой-то коммит с ветки master}
\end{itemize}

Для проведения эксперимента была создана тестовая таблица, состоящая из 10 столбцов и 100 миллионов строк, занимающая 12Гб места на диске. \todo{Схема таблицы? Ссылка на код заполнения?} Такие размеры необходимы для того, чтобы показать преимущество использования \pgduckdb{}: чем больше данных, тем эффективнее работа \pgduckdb{} при \olap{}-нагрузке. Эти данные также были конвертированы в формат данных \parquet{} с использованием \pgparquet{} --- итоговый .parquet файл занимает 3.5Гб дискового пространства \footnote{Файлы в формате .csv и .parquet, содержащие тестовые данные, можно найти по ссылке \url{https://drive.google.com/file/d/1su-JGOgVIV-UdxJV2RavULdccnonY9W9/view?usp=drive_link} (дата обращения: \DTMdate{2026-01-15}).}.

\todo{Также были созданы индексы в тестовой таблице... Расписать подробнее, какие индексы и какое преимущество они дают. Плюс сослаться на \cite{postgresqlInside} Часть V. Типы индексов}.

\todo{Где-то сослаться на \cite{professionalSQL}}.

При проведении серии экспериментов сравнивались следующие три различные конфигурации:
\begin{enumerate}
    \item \enquote{\postgresql{}}~--- \enquote{классический} \postgresql{} версии 18.1 без аналитических расширений, работающий с данными, представленными в стандартном для \sql{} \todo{виде (таблицах);}
    \item \enquote{\pgduckdb{} и таблица}~--- \postgresql{} с использованием расширения \pgduckdb{} (в конфигурации \postgresql{} предварительно устанавливается \todo{опция} \foreignquote{english}{pgduckdb.force\_execution=true}), запросы всё так же выполняются над таблицей;
    \item \enquote{\pgduckdb{} и \parquet{}}~--- \postgresql{} с использованием расширения \pgduckdb{}, запросы выполняются над .parquet файлом.
\end{enumerate}

При сравнении скорости работы различных подходов использовались следующие запросы:
\begin{enumerate}
    \item аналитический запрос, использующий оконные функции \todo{сноска/ссылка(https://postgrespro.ru/docs/postgrespro/current/tutorial-window)}, сортировку и группировку значений;
    \item фильтрационный запрос, использующий построенные индексы;
    \item \todo{обычный \enquote{SELECT *} запрос}.
\end{enumerate}
\todo{Примеры запросов можно найти ...}

\todo{о том, как проводились эксперименты?}



\subsection{Исследовательские вопросы}

\begin{description}
    \item[RQ1]: насколько использование формата данных \parquet{} ускоряет выполнение запросов в \postgresql{}?
    \item[RQ2]: при какой конфигурации использование \pgduckdb{} дает замедление в выполнении запросов?
\end{description}


\subsection{Метрики}
В данной серии экспериментов за метрику принимается время выполнения запроса в миллисекундах. \todo{Данное время получается в \postgresql{} использованием мета-команды} \verb=\timing on= \cite{dbBasics}.

\subsection{Результаты}
В результате проведения экспериментов на тестовом стенде получились следующие результаты, представленные в таблицах \ref{tab:bigquery}, \ref{tab:index} и \ref{tab:select} и на рис. \ref{fig:bigquery} и \ref{fig:select}.

Все полученные данные прошли тесты на нормальность \todo{ссылка на умную статью/книгу про Шапиро и чтото еще}, погрешность измерений не превышает \todo{стольки-то процентов}.

\begin{table}[h]
\centering
\caption{Результаты выполнения аналитического запроса.}
\label{tab:bigquery}
\begin{tabular}{|c|c|}
\hline
Конфигурация & $t$, мс \\
\hline
\hline
\postgresql{} & 27730 ± 90 \\
\hline
\pgduckdb{} и таблица & 11410 ± 40 \\
\hline
\pgduckdb{} и \parquet{} & 1938 ± 5  \\
\hline
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\caption{Результаты выполнения запроса с индексом.}
\label{tab:index}
\begin{tabular}{|c|c|}
\hline
Конфигурация & $t$, мс \\
\hline
\hline
\postgresql{} & 1.023 ± 0.010 \\
\hline
\pgduckdb{} и таблица & 109.9 ± 1.0 \\
\hline
\pgduckdb{} и \parquet{} & 1669 ± 4  \\
\hline
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\caption{Результаты выполнения select-запроса.}
\label{tab:select}
\begin{tabular}{|c|c|}
\hline
Конфигурация & $t$, мс \\
\hline
\hline
\postgresql{} & 11190 ± 70 \\
\hline
\pgduckdb{} и таблица & 18380 ± 40  \\
\hline
\pgduckdb{} и \parquet{} & 5025 ± 11  \\
\hline
\end{tabular}
\end{table}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{figures/chart-big-query.png}
    \caption{График времени выполнения аналитического запроса.}
    \label{fig:bigquery}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{figures/chart-select.png}
    \caption{График времени выполнения select-запроса.}
    \label{fig:select}
\end{figure}



\subsubsection{RQ1}
Как можно видеть из \todo{таблицы \ref{tab:bigquery} и рис. \ref{fig:bigquery}}, использование \pgduckdb{} при выполнении аналитических запросов дает существенный выигрыш в скорости: \todo{над таблицей} \pgduckdb{} работает быстрее \postgresql{} в 2.4 раза; \todo{если же при этом еще использовать представление данных в формате \parquet{}}, аналитический запрос выполняется в 14.3 раз быстрее в сравнении с \enquote{классическим} подходом \postgresql{}.

Также стоит заметить, что выполнение \todo{про select psql vs pgduckdb+parquet}
\todo{Ссылка, почему \pgduckdb{} быстрее даже на \enquote{select *}: \url{https://www.crunchydata.com/blog/how-we-fused-duckdb-into-postgres-with-crunchy-bridge-for-analytics}}

\subsubsection{RQ2}
Исходя из результатов, представленных в \todo{таблице \ref{tab:index}}, можно заключить, что \todo{при должном построении индексов и выполнении запросов, направленных на "поиск", стандартная конфигурация \postgresql{} все еще дает значительно лучшие результаты: \pgduckdb{} приводит к замедлению в более, чем 100 раз, на таблицах и к замедлению более, чем в 1500 раз, при использовании формата данных \parquet{}.}
